require:
  - beet.contrib.extra_files

pipeline:
  - directory: 'base'
    extend: 'beet.yaml'
  - gm4.plugins.versioning.modules
  - gm4.plugins.upgrade_paths

meta:
  extra_files:
    data_pack: ['README.md']
