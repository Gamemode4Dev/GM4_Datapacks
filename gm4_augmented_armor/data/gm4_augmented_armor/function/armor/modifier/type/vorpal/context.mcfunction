# schedule is needed to let teleport work, because minecraft, that's why
# @s = unspecified
# at unspecified
# run from armor/type/vorpal/schedule

# regain context
execute as @a[tag=gm4_aa_vorpal_start] at @s run function gm4_augmented_armor:armor/modifier/type/vorpal/attempt
